# Audios generados localmente (42 MB — regenerar con make_audio.py)
audio/

# Códigos de activación privados — NUNCA subir al repositorio
activation_codes_PRIVATE.json
../activation_codes_PRIVATE.json

# Scripts de generación (opcionales, contienen lógica interna)
make_codes.py
make_icons.py
make_explanations.py

# Capacitor: www/ se regenera con scripts/build-www.mjs
www/

# Node / sistema
node_modules/
.DS_Store
Thumbs.db

# Vercel
.vercel/

# Variables de entorno (API key de Anthropic)
.env
.env.local
.env.*.local
